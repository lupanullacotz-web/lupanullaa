# Components

Reusable HTML snippets for Lupanulla pages can live here.

- `pwa-head.html` contains favicon, Apple icon, manifest, and theme-color tags.
- Existing root snippets such as `navbar.html`, `footer.html`, and `sidebar.html` can be moved or copied here when pages are converted to component includes.
